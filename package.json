{
  "name": "@oomia/upside-template",
  "author": "ooMia",
  "version": "0.0.2",
  "description": "upside_practice_project",
  "license": "UNLICENSED",
  "scripts": {
    "preinstall": "forge install && forge update && yarn remapping",
    "test": "yarn format && forge test",
    "format": "yarn compile && prettier --write . && forge fmt",
    "build": "forge build",
    "compile": "forge compile",
    "remapping": "forge remappings > remappings.txt"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-solidity": "^1.4.1"
  },
  "engines": {
    "node": ">=22.8.0"
  },
  "packageManager": "yarn@4.4.1"
}
